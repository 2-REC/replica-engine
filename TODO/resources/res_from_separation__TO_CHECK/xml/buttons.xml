<?xml version="1.0" encoding="utf-8"?>

<!-- !!!! OUYA only !!!! -->

<!--
  buttons:
  Specifies the buttons configuration for the OUYA controller.
  This file is mandatory.

  !!!! CAUTION:
  All the buttons defined here also have to be defined in "InputGameInterfaceImpl" (and in the same order!).

  Example:
    private static final int JUMP_BUTTON = 0;
    public final InputButton getJumpButton()
    {
        return getButton( JUMP_BUTTON );
    }


    - button (M) (1+) :
      A configurable button.
      - key     (M) : preference key (used for Preferences management) (string)
      - default (M) : default OUYA button (int)
      - name    (M) : name of the associated action (string resource)


    Example:

    <buttons>
      <button key="buttonJump" default="96" name="@string/preference_controls_jump" />
      <button key="buttonAttack" default="99" name="@string/preference_controls_attack" />
    </buttons>

-->
